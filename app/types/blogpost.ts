export type Blogpost = {
    id: string
    header: string
    description: string
    keywords: string[]
    content: string
    created_at: string
}

export type StoreBlogpost = {
    header: string
    description: string
    keywords: string[]
    content: string
}