export type User = {
    id: string
    first_name: string
    last_name: string
    email: string
    password: string
    created_at: string
}

export type StoreUser = {
    first_name: string
    last_name: string
    email: string
    password: string
}